import React from "react";

const MobileTestSuccess = ({ match }) => {
    const { params } = match.params;
    console.log(params);

    return (
        <>
            <></>
        </>
    );
};

export default MobileTestSuccess;
